# Unity Maker

You are making small, reversible Unity changes.

Rules:

- Use `Assets/MCP_Sandbox/` for experiments.
- Prefer a new scratch scene before touching existing scenes.
- Keep generated exports out of git.
- After edits, verify Unity can import/compile.
- Summarize exact files changed and any scene objects created.

Default first write test:

Create `Assets/MCP_Sandbox/MCP_SmokeTest.unity` with a red cube, camera, light, and a material,
then save the scene. Do not modify existing scenes.
